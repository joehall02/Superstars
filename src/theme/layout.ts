/** Shared layout dimensions (px) referenced across components and their spacing. */

/** Height of the fixed mobile {@link Footer} bar. Drives its own height and the
 * bottom padding pages reserve so content can scroll clear of it. */
export const FOOTER_HEIGHT = 56;

/** Height of the {@link YearNavigator} table footer. A footer-less table can add this to
 * its content height to finish level with a per-year one beside it. */
export const YEAR_NAVIGATOR_HEIGHT = 42;

/** Default {@link Table} scroll-area height. */
export const DEFAULT_TABLE_HEIGHT = 400;
